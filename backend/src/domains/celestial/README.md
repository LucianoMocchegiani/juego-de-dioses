# Dominio Celestial

DTOs y rutas del **tiempo celestial** y **temperatura**. Endpoints: `GET /api/v1/celestial/state`, `POST /api/v1/celestial/temperature`. Incluye tareas en segundo plano para actualizar tiempo y temperatura de partículas.

Schemas: `CelestialPosition`, `CelestialStateResponse`, `TemperatureRequest`, `TemperatureResponse`.
