# Dominio Characters

DTOs y rutas de **personajes** (bípedos). Endpoints: `GET/POST /api/v1/bloques/{id}/characters`, `GET .../characters/{id}/model`. Usa `GeometriaVisual` y `parse_jsonb_field` desde `shared`.

Schemas: `BipedGeometryPart`, `BipedGeometry`, `Model3DOffset`, `Model3DRotation`, `Model3D`, `CharacterResponse`, `CharacterCreate`.
