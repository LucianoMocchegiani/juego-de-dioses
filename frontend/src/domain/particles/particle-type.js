/**
 * Modelo de dominio: Tipo de partícula
 * Forma canónica de lo que devuelve el backend para un tipo de partícula.
 * Sin dependencias de Three.js, fetch ni DOM.
 */

/**
 * @typedef {Object} ParticleType
 * @property {string} id
 * @property {string} [nombre]
 * @property {string} [color]
 * @property {string} [geometria]
 * @property {number} [opacidad]
 */

export const ParticleType = {};
